package ${coder.packageName}.dao;

import java.util.List;

import tk.mybatis.mapper.common.Mapper;
import ${coder.packageName}.model.${coder.className};
 /** 
 * @see ${coder.title}dao接口类
 * @author Suny
 * @date ${coder.crtDate?string("yyyy/MM/dd")}
 */
public interface ${coder.className}Dao extends Mapper<${coder.className}> {
	//int save(${coder.className} ${coder.classDeclare});

	//int update(${coder.className} ${coder.classDeclare});

	//int delete(String ${coder.classDeclare}Id);
	
	//void findForPageList(Page<DbTable> page);
}